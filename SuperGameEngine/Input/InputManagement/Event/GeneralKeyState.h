#pragma once
#include <vector>
#include <string>


// +------------------------------+
// Generated by SuperGenerator as an
// SuperEnum. See Tools for details.
// +------------------------------+
namespace SuperGameInput
{
    /// <summary>
    /// A more general key state used across various input types.
    /// </summary>
    enum class GeneralKeyState : int8_t
    {
        /// <summary>
        /// Only used for unsuccessful parsing.
        /// </summary>
        Unknown = -1,

        SDL__RELEASED,

        SDL__PRESSED,
    };

    /// <summary>
    /// Accompanies enums to provide extra functionality.
    /// </summary>
    class EGeneralKeyState
    {
    public:
        static GeneralKeyState Min() { return GeneralKeyState::SDL__RELEASED; }
        static GeneralKeyState Max() { return GeneralKeyState::SDL__PRESSED; }

        static GeneralKeyState* ToArray()
        {
            static GeneralKeyState returnArray[] =
            {
                GeneralKeyState::SDL__RELEASED,
                GeneralKeyState::SDL__PRESSED,
            };
            
            return returnArray;
        }

        static std::vector<GeneralKeyState> ToVector()
        {
            static std::vector<GeneralKeyState> returnVector =
            {
                GeneralKeyState::SDL__RELEASED,
                GeneralKeyState::SDL__PRESSED,
            };
            
            return returnVector;
        }

        static std::vector<std::string> ToVectorValues()
        {
            static std::vector<std::string> returnVector =
            {
                "SDL__RELEASED",
                "SDL__PRESSED",
            };
            
            return returnVector;
        }

        static std::string ToString(GeneralKeyState value)
        {
            switch (value)
            {
                case GeneralKeyState::Unknown: return "Unknown";
                case GeneralKeyState::SDL__RELEASED: return "SDL__RELEASED";
                case GeneralKeyState::SDL__PRESSED: return "SDL__PRESSED";
            }
            
            return "Unknown";
        }

        static GeneralKeyState FromString(std::string value, bool checkCase = true)
        {
            if (checkCase)
            {
                if (value == "Unknown") return GeneralKeyState::Unknown;
                if (value == "SDL__RELEASED") return GeneralKeyState::SDL__RELEASED;
                if (value == "SDL__PRESSED") return GeneralKeyState::SDL__PRESSED;
            }
            else
            {
                std::string valueLower = ToLower(value); 
                if (valueLower == "unknown") return GeneralKeyState::Unknown;
                if (valueLower == "sdl__released") return GeneralKeyState::SDL__RELEASED;
                if (valueLower == "sdl__pressed") return GeneralKeyState::SDL__PRESSED;
            }
            
            return GeneralKeyState::Unknown;
        }
    
 private:

        /// <summary>
        /// Converts to lower. Copy from StringHelpers to ensure Enum does not require
        /// any outside dependency. Although we could include a header, doing so restricts
        /// the project which holds StringHelpers.
        /// </summary>
        static std::string ToLower(const std::string & input)
        {
            std::string output = input;
            for (char& c : output)
            {
                if (c >= 'A' && c <= 'Z')
                {
                    c = c - 'A' + 'a';
                }
            }
            return output;
        }
    };
}
