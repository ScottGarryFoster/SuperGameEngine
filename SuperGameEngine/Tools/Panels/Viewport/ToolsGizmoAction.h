#pragma once
#include <vector>
#include <string>


// +------------------------------+
// Generated by SuperGenerator as an
// SuperEnum. See Tools for details.
// +------------------------------+
namespace SuperGameTools
{
    /// <summary>
    /// Describes the action to take when reacting to a Gizmo event
    /// </summary>
    enum class ToolsGizmoAction : uint8_t
    {
        /// <summary>
        /// Only used for unsuccessful parsing.
        /// </summary>
        Unknown = 0,

        /// <summary>
        /// The gizmo is selected.
        /// </summary>
        GizmoSelected,

        /// <summary>
        /// The gizmo is unselected.
        /// </summary>
        GizmoUnselected,

        /// <summary>
        /// Move by the amount given.
        /// </summary>
        MoveBy,
    };

    /// <summary>
    /// Accompanies enums to provide extra functionality.
    /// </summary>
    class EToolsGizmoAction
    {
    public:
        static ToolsGizmoAction Min() { return ToolsGizmoAction::GizmoSelected; }
        static ToolsGizmoAction Max() { return ToolsGizmoAction::MoveBy; }

        static ToolsGizmoAction* ToArray()
        {
            static ToolsGizmoAction returnArray[] =
            {
                ToolsGizmoAction::GizmoSelected,
                ToolsGizmoAction::GizmoUnselected,
                ToolsGizmoAction::MoveBy,
            };
            
            return returnArray;
        }

        static std::vector<ToolsGizmoAction> ToVector()
        {
            static std::vector<ToolsGizmoAction> returnVector =
            {
                ToolsGizmoAction::GizmoSelected,
                ToolsGizmoAction::GizmoUnselected,
                ToolsGizmoAction::MoveBy,
            };
            
            return returnVector;
        }

        static std::vector<std::string> ToVectorValues()
        {
            static std::vector<std::string> returnVector =
            {
                "GizmoSelected",
                "GizmoUnselected",
                "MoveBy",
            };
            
            return returnVector;
        }

        static std::string ToString(ToolsGizmoAction value)
        {
            switch (value)
            {
                case ToolsGizmoAction::Unknown: return "Unknown";
                case ToolsGizmoAction::GizmoSelected: return "GizmoSelected";
                case ToolsGizmoAction::GizmoUnselected: return "GizmoUnselected";
                case ToolsGizmoAction::MoveBy: return "MoveBy";
            }
            
            return "Unknown";
        }

        static ToolsGizmoAction FromString(const std::string& value, bool checkCase = true)
        {
            if (checkCase)
            {
                if (value == "Unknown") return ToolsGizmoAction::Unknown;
                if (value == "GizmoSelected") return ToolsGizmoAction::GizmoSelected;
                if (value == "GizmoUnselected") return ToolsGizmoAction::GizmoUnselected;
                if (value == "MoveBy") return ToolsGizmoAction::MoveBy;
            }
            else
            {
                std::string valueLower = ToLower(value); 
                if (valueLower == "unknown") return ToolsGizmoAction::Unknown;
                if (valueLower == "gizmoselected") return ToolsGizmoAction::GizmoSelected;
                if (valueLower == "gizmounselected") return ToolsGizmoAction::GizmoUnselected;
                if (valueLower == "moveby") return ToolsGizmoAction::MoveBy;
            }
            
            return ToolsGizmoAction::Unknown;
        }
    
 private:

        /// <summary>
        /// Converts to lower. Copy from StringHelpers to ensure Enum does not require
        /// any outside dependency. Although we could include a header, doing so restricts
        /// the project which holds StringHelpers.
        /// </summary>
        static std::string ToLower(const std::string & input)
        {
            std::string output = input;
            for (char& c : output)
            {
                if (c >= 'A' && c <= 'Z')
                {
                    c = c - 'A' + 'a';
                }
            }
            return output;
        }
    };
}
