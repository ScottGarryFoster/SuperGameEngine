#pragma once
#include <vector>
#include <string>


// +------------------------------+
// Generated by SuperGenerator as an
// SuperEnum. See Tools for details.
// +------------------------------+
namespace SuperGameTools
{
    /// <summary>
    /// Describes what updated (or how) when the scene updates.
    /// </summary>
    enum class SceneUpdateAction : uint8_t
    {
        /// <summary>
        /// Only used for unsuccessful parsing.
        /// </summary>
        Unknown,

        /// <summary>
        /// The scene has been loaded or replaced with another.
        /// </summary>
        NewScene,
    };

    /// <summary>
    /// Accompanies enums to provide extra functionality.
    /// </summary>
    class ESceneUpdateAction
    {
    public:
        static SceneUpdateAction Min() { return SceneUpdateAction::NewScene; }
        static SceneUpdateAction Max() { return SceneUpdateAction::NewScene; }

        static SceneUpdateAction* ToArray()
        {
            static SceneUpdateAction returnArray[] =
            {
                SceneUpdateAction::NewScene,
            };
            
            return returnArray;
        }

        static std::vector<SceneUpdateAction> ToVector()
        {
            static std::vector<SceneUpdateAction> returnVector =
            {
                SceneUpdateAction::NewScene,
            };
            
            return returnVector;
        }

        static std::vector<std::string> ToVectorValues()
        {
            static std::vector<std::string> returnVector =
            {
                "NewScene",
            };
            
            return returnVector;
        }

        static std::string* ToArrayValues()
        {
            static std::string returnArray[] =
            {
                "NewScene",
            };
            
            return returnArray;
        }

        static std::string ToString(SceneUpdateAction value)
        {
            switch (value)
            {
                case SceneUpdateAction::Unknown: return "Unknown";
                case SceneUpdateAction::NewScene: return "NewScene";
            }
            
            return "Unknown";
        }

        static SceneUpdateAction FromString(const std::string& value, bool checkCase = true)
        {
            if (checkCase)
            {
                if (value == "Unknown") return SceneUpdateAction::Unknown;
                if (value == "NewScene") return SceneUpdateAction::NewScene;
            }
            else
            {
                std::string valueLower = ToLower(value); 
                if (valueLower == "unknown") return SceneUpdateAction::Unknown;
                if (valueLower == "newscene") return SceneUpdateAction::NewScene;
            }
            
            return SceneUpdateAction::Unknown;
        }
    
 private:

        /// <summary>
        /// Converts to lower. Copy from StringHelpers to ensure Enum does not require
        /// any outside dependency. Although we could include a header, doing so restricts
        /// the project which holds StringHelpers.
        /// </summary>
        static std::string ToLower(const std::string & input)
        {
            std::string output = input;
            for (char& c : output)
            {
                if (c >= 'A' && c <= 'Z')
                {
                    c = c - 'A' + 'a';
                }
            }
            return output;
        }
    };
}
