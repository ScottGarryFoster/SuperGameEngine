#pragma once
#include <vector>
#include <string>


// +------------------------------+
// Generated by SuperGenerator as an
// SuperEnum. See Tools for details.
// +------------------------------+
namespace SuperEnumGenerator
{
    /// <summary>
    /// How to create the enum from a broad perspective.
    /// </summary>
    enum class SuperEnumType : int8_t
    {
        /// <summary>
        /// Only used for unsuccessful parsing.
        /// </summary>
        Unknown = -1,

        /// <summary>
        /// Values are given as is, in order.
        /// </summary>
        Standard,

        /// <summary>
        /// Values are a part of bit flags and in order must add numerically.
        /// </summary>
        BitFlag,
    };

    /// <summary>
    /// Accompanies enums to provide extra functionality.
    /// </summary>
    class ESuperEnumType
    {
    public:
        static SuperEnumType Min() { return SuperEnumType::Standard; }
        static SuperEnumType Max() { return SuperEnumType::BitFlag; }

        static SuperEnumType* ToArray()
        {
            static SuperEnumType returnArray[] =
            {
                SuperEnumType::Standard,
                SuperEnumType::BitFlag,
            };
            
            return returnArray;
        }

        static std::vector<SuperEnumType> ToVector()
        {
            static std::vector<SuperEnumType> returnVector =
            {
                SuperEnumType::Standard,
                SuperEnumType::BitFlag,
            };
            
            return returnVector;
        }

        static std::string ToString(SuperEnumType value)
        {
            switch (value)
            {
                case SuperEnumType::Unknown: return "Unknown";
                case SuperEnumType::Standard: return "Standard";
                case SuperEnumType::BitFlag: return "BitFlag";
            }
            
            return "Unknown";
        }

        static SuperEnumType FromString(std::string value, bool checkCase = true)
        {
            if (checkCase)
            {
                if (value == "Unknown") return SuperEnumType::Unknown;
                if (value == "Standard") return SuperEnumType::Standard;
                if (value == "BitFlag") return SuperEnumType::BitFlag;
            }
            else
            {
                std::string valueLower = ToLower(value); 
                if (valueLower == "unknown") return SuperEnumType::Unknown;
                if (valueLower == "standard") return SuperEnumType::Standard;
                if (valueLower == "bitflag") return SuperEnumType::BitFlag;
            }
            
            return SuperEnumType::Unknown;
        }

        /// <summary>
        /// Converts to lower. Copy from StringHelpers to ensure Enum does not require
        /// any outside dependency. Although we could include a header, doing so restricts
        /// the project which holds StringHelpers.
        /// </summary>
        static std::string ToLower(const std::string & input)
        {
            std::string output = input;
            for (char& c : output)
            {
                if (c >= 'A' && c <= 'Z')
                {
                    c = c - 'A' + 'a';
                }
            }
            return output;
        }
    };
}
